fastapi
uvicorn[standard]
python-multipart
aiofiles
sse-starlette
requests
pydantic>=2
pdf2image
Pillow
python-dotenv

# cài thêm để hỗ trợ render LaTeX
# sudo apt install -y texlive-xetex texlive-fonts-recommended texlive-fonts-extra

# LLM backend (cài riêng theo CUDA — xem deploy/install_server.sh):
#   pip install "sglang[all]"
