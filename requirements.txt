fastapi
uvicorn[standard]
python-multipart
aiofiles
sse-starlette
requests
pydantic>=2
pdf2image
Pillow
python-dotenv

# LLM backend (cài riêng theo CUDA — xem deploy/install_server.sh):
#   pip install "sglang[all]"
